fruits=['apples','pears','strawberry']
for fruit in fruits:
    if fruit=='pears':
        print(fruit)
    else:
        print('not pears')

for x in range(0,3): #len(fruits) can be done instead of number
    if fruits[x]=='pears':
        print(fruits[x])
    else:
        print('not pears')
